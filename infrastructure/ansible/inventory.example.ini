# Copy to inventory.ini — use your edge host SSH target (no secrets in VCS).
[edge_gateway]
edge-local ansible_host=127.0.0.1 ansible_user=${EDGE_SSH_USER} ansible_ssh_private_key_file=${EDGE_SSH_KEY_PATH}
